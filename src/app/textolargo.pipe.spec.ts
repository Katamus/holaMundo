import { TextolargoPipe } from './textolargo.pipe';

describe('TextolargoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextolargoPipe();
    expect(pipe).toBeTruthy();
  });
});
